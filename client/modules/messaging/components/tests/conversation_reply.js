const { describe, it } = global;
import { expect } from 'chai';
import { shallow } from 'enzyme';
import ConversationReply from '../conversation_reply';

describe('messaging.components.conversation_reply', () => {
  it('should do something');
});
