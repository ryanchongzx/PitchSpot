const { describe, it } = global;
import { expect } from 'chai';
import { shallow } from 'enzyme';
import SettingsPasswordForgot from '../settings_password_forgot';

describe('users.components.settings_password_forgot', () => {
  it('should do something');
});
