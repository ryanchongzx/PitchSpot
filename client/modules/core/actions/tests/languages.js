const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../languages';

describe('core.actions.languages', () => {
  it('should do something');
});
